import React from "react";

const Taster = () => {
  return (
    <div>
      Taster Form here, could have different behaviour if the user is not signed
      in?
      <br />
      Maybe we make them create an account before we let them fill out a taster
      form?
    </div>
  );
};

export default Taster;
